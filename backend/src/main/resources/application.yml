server:
  port: 8080

spring:
  application:
    name: home-lab-backend

# Phase 2: optional – set in application-local.yml or env (do not commit secrets)
homelab:
  server-agent-url:    # e.g. http://192.168.1.x:5000 – leave blank for mock
  desktop-agent-url: http://192.168.0.235:5001
  unifi:
    enabled: false
    base-url:         # e.g. https://192.168.1.1:8443
    username:
    password:
    use-unifi-os: false   # true for UDM/UniFi OS; false for UniFi Network Application
